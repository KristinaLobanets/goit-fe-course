export { markUp };
// const menuLi = array.map(item => template(item)).join('');

// const menuLi = array.reduce((acc, item) => acc + template(item), '');

// const menuLi = template(array);

// menuUl.insertAdjacentHTML('beforeend', menuLi);
